# 🧩 HACS Installation Instructions

## Manual Installation
	1.	Download the entire repository as a ZIP file or clone it:

git clone https://github.com/chinedu40/hacs_enphase_envoy_cloud.git


	2.	Copy or move the folder hacs_enphase_envoy_cloud into your Home Assistant custom_components directory.
The final path should look like this:

/config/custom_components/hacs_enphase_envoy_cloud/


	3.	Restart Home Assistant to load the integration.
	4.	The integration should now be available under Settings → Devices & Services → Integrations → Add Integration → Enphase Envoy Cloud.

⸻

🔄 Updating

When updating manually, simply replace the existing folder inside
/config/custom_components/hacs_enphase_envoy_cloud/ with the new version and restart Home Assistant.
